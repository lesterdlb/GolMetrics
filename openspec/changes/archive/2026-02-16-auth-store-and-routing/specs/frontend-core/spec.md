# Frontend Core (Delta)

No new, modified, or removed requirements. This change implements existing requirements from the `frontend-core` spec:

- **Routing** (route definitions, protected route redirects, auth route redirects, root path redirect)
- **State Management** (auth store shape, token persistence, logout behavior)
- **HTTP Client** (base URL configuration, authorization header injection, unauthorized response handling)

All requirements remain unchanged from `openspec/specs/frontend-core/spec.md`.
